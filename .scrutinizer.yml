build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run
            environment:
                php:
                    version: 8.3.16

filter:
    paths: ["src/*"]
